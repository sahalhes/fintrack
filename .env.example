# Example environment file (safe placeholders)
# Copy to .env.local and populate real secrets locally or via CI/CD secrets.

# Analytics (public)
NEXT_PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Auth0 (server-side secrets must be kept secret)
AUTH0_SECRET=your-auth0-secret
AUTH0_CLIENT_SECRET=your-auth0-client-secret
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
APP_BASE_URL=http://localhost:3001

# Set to 'true' only after Auth0 is configured and working
NEXT_PUBLIC_AUTH0_ENABLED=false

# MongoDB
DATABASE_URL=mongodb://localhost:27017/fintrack
MONGODB_DB_NAME=fintrack